# Core dependencies for AST SpaceMobile Satellite Analysis Library
# Python >= 3.8 required

# Numerical computing
numpy>=1.20.0

# Data manipulation and CSV export
pandas>=1.3.0

# HTTP requests for TLE data fetching
requests>=2.26.0

# Satellite orbital mechanics (SGP4 propagator)
skyfield>=1.42

# Visualization and graph generation
matplotlib>=3.4.0

# Optional: PDF generation (for legacy scripts)
# Uncomment if you need PDF report generation
# markdown>=3.3.0
# weasyprint>=52.5
